#ifndef COMPOUND_TIMEDATE_H
# define COMPOUND_TIMEDATE_H

# include <Compound/string.h>

typedef enum {
  UTC = 0,
  /* etc. */
} TimeZone;

typedef struct {
  
} TimeDate;

#endif  /* COMPOUND_TIMEDATE_H */
